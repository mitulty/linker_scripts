# Instructions

1. Execute `make` to generate the `.o`, `.elf`, `.map` and `.txt` files.
   a. `.map` file has the placement related information.
   b. `.txt` saves the output of the `readelf` reading the generated `.elf`.
2. `make clean` deletes the files generated during compilation.
